💾 Как настроить постоянное хранилище для Qdrant
Вы можете выбрать один из двух способов, чтобы данные сохранялись между запусками контейнера.

1. Простая команда docker run
Добавьте флаг -v для подключения папки с вашего компьютера в контейнер:

bash
docker run -p 6333:6333 \
  -v $(pwd)/qdrant_storage:/qdrant/storage:z \
  qdrant/qdrant
$(pwd)/qdrant_storage — путь к папке на вашем компьютере (например, в текущей директории).

/qdrant/storage — путь внутри контейнера, где Qdrant хранит данные по умолчанию.

При остановке контейнера все данные остаются в папке qdrant_storage. При следующем запуске этой же командой Qdrant загрузит все данные из этой папки.

2. Использование Docker Compose (рекомендуется)
Этот способ удобнее для управления. Создайте файл docker-compose.yml:

yaml
services:
  qdrant:
    image: qdrant/qdrant:latest
    container_name: qdrant
    ports:
      - "6333:6333"
      - "6334:6334"
    volumes:
      - qdrant_storage:/qdrant/storage  # Использование named volume
    restart: unless-stopped  # Автоматический перезапуск

volumes:
  qdrant_storage:  # Volume будет создан автоматически
Запустите его командой docker-compose up -d. Docker сам создаст постоянное хранилище с именем qdrant_storage, и данные будут сохраняться после перезапусков.

💡 Важные моменты при настройке
Проверка работоспособности: После запуска убедитесь, что Qdrant работает, проверив его API-эндпоинт: curl http://localhost:6333.

Что происходит без volume: Если запустить контейнер без указания тома (-v), все данные будут храниться только внутри контейнера и удалятся вместе с ним.

Настроив постоянное хранилище одним из этих способов, вы можете быть уверены, что ваши векторные эмбеддинги, созданные для Kilo Code, будут надежно сохранены.