# ⚙️ Основные настройки: RAM вместо диска
storage:
  on_disk_payload: false
  on_disk_vectors: false
  # Этот параметр был в docker-compose.yml
  strict_mode: false

# 🔧 Оптимизаторы: немедленное индексирование и отключение агрессивной очистки
optimizers:
  # индекс сразу после 1 точки
  indexing_threshold: 1
  # разрешить scroll без индекса до 10 точек
  full_scan_threshold: 10
  # Минимальная доля удаленных векторов в сегменте, необходимая для оптимизации
  deleted_threshold: 0.1
  # Минимальное количество векторов в сегменте, необходимое для оптимизации
  vacuum_min_vector_number: 100

# 🧠 HNSW: компактный, RAM-only, быстрый старт
hnsw_config:
  # меньше → меньше памяти, быстрее
  m: 16
  ef_construct: 100
  on_disk: false
  full_scan_threshold: 10

# 🚫 Отключаем излишние фоновые процессы
wal:
  wal_capacity_mb: 32

telemetry_disabled: true